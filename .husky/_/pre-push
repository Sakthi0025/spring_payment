echo "Running full local build before push..."

# Run full Maven lifecycle (compile, test, package, verify)
mvn clean verify

# Check result
if [ $? -ne 0 ]; then
  echo "Build or tests failed. Push blocked."
  exit 1
fi

echo "Build successful. Push allowed."